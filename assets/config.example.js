// ==========================================
// CONFIGURATION PORTFOLIO - EXEMPLE
// ==========================================
// Copiez ce fichier vers "config.js" et ajoutez votre propre token GitHub

window.PORTFOLIO_CONFIG = {
  // Token GitHub pour le chatbot IA
  // Créez un token sur : https://github.com/settings/tokens
  // Scopes requis : repo (lecture), workflow (si CI/CD)
  GITHUB_TOKEN: "VOTRE_GITHUB_TOKEN_ICI"
};
